% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validators.R
\name{check_spatial_compatible}
\alias{check_spatial_compatible}
\title{Check if spatial objects are compatible}
\usage{
check_spatial_compatible(
  x,
  y,
  check_crs = TRUE,
  check_overlap = FALSE,
  name_x = "x",
  name_y = "y"
)
}
\arguments{
\item{x}{First spatial object (SpatRaster, SpatVector, or sf)}

\item{y}{Second spatial object (SpatRaster, SpatVector, or sf)}

\item{check_crs}{Logical, check if CRS match (default: TRUE)}

\item{check_overlap}{Logical, check if extents overlap (default: FALSE)}

\item{name_x}{Character string, name of first object (for error messages)}

\item{name_y}{Character string, name of second object (for error messages)}
}
\value{
Invisible TRUE if valid, otherwise stops with error
}
\description{
Validates that two spatial objects have compatible properties
(e.g., same CRS, overlapping extents).
}
\examples{
\dontrun{
if (requireNamespace("terra", quietly = TRUE)) {
  r1 <- terra::rast(ncol=10, nrow=10)
  r2 <- terra::rast(ncol=10, nrow=10)
  terra::crs(r1) <- terra::crs(r2) <- "EPSG:4326"
  check_spatial_compatible(r1, r2)
}
}
}
