% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_netcdf.R
\name{read_nc_data}
\alias{read_nc_data}
\title{Read NetCDF data with spatial subsetting}
\usage{
read_nc_data(
  ncid,
  variables = NULL,
  extent = NULL,
  format = c("modern", "legacy")
)
}
\arguments{
\item{ncid}{NetCDF connection object from ncdf4::nc_open()}

\item{variables}{Character vector of variable names to read (NULL = all)}

\item{extent}{Numeric vector c(xmin, xmax, ymin, ymax) for spatial subset}

\item{format}{Character. Output format ("modern" uses terra, "legacy" uses raster)}
}
\value{
List containing coordinates, data array, and time information
}
\description{
Reads NetCDF data with optional spatial and variable subsetting.
Supports both legacy raster and modern terra output formats.
}
\examples{
\dontrun{
library(ncdf4)
nc <- nc_open("climate_data.nc")

# Read all variables
data <- read_nc_data(nc)

# Read specific variables with spatial subset
extent <- c(-180, -120, 30, 60)  # Western North America
data <- read_nc_data(nc, variables = c("temp", "precip"), extent = extent)

# Use modern terra format
data <- read_nc_data(nc, format = "modern")

nc_close(nc)
}
}
\seealso{
Other netcdf-io: 
\code{\link{get_nc_info}()},
\code{\link{nc_to_raster}()},
\code{\link{open_nc_file}()},
\code{\link{read_nc_time}()}
}
\concept{netcdf-io}
