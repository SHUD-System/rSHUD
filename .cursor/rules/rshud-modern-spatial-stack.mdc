---
description: 空间栈现代化——优先 sf/terra，减少旧空间包依赖并检查上下游兼容性
alwaysApply: true
---

# rSHUD 空间依赖现代化

## 目标

- 改动代码时，优先采用 `sf` 与 `terra` 的数据结构和 API。
- 在可行范围内，逐步移除或避免新增对旧空间包的依赖：`raster`、`sp`、`rgdal`、`rgeos`。

## 执行要求

1. 设计与实现新逻辑时，默认使用 `sf`/`terra`（如 `sf` 几何对象、`terra::rast()`、`terra::vect()`）。
2. 非必要不新增 `raster`/`sp`/`rgdal`/`rgeos` 调用；若必须保留，需说明原因与后续替换路径。
3. 修改函数时，检查并说明**上游兼容性**（输入类型、CRS 约定、字段命名、单位）与**下游兼容性**（返回类型、列名、维度、调用方行为）。
4. 优先最小可行改动：避免一次性大重构；迁移时可保留短期兼容分支，但需明确退出条件。
5. 提交结果时应给出兼容性结论：哪些接口保持不变、哪些行为变化、调用方是否需要调整。

## 参数命名（`sf` 几何）

- 凡**类型约定为 `sf`** 的形参，命名使用 **`sf_` 前缀**（蛇形命名），例如 `sf_mesh`、`sf_riv`、`sf_boundary`。
- **不要**再用 `sp.`、`sp_` 等容易与 **sp 包 / `Spatial*`** 混淆的前缀表示 `sf` 对象。
- **重命名对照（迁移时）**：`sp.mesh` → `sf_mesh`，`sp.riv`（实为线状 `sf`）→ `sf_riv`；其他同理，用 `sf_<语义>`。
- 若过渡期仍接受 `Spatial*`，应在参数名与文档中明确为 `sp_*` 或写明 `Spatial*`，与 `sf_*` 区分；新写接口优先只暴露 `sf_*`。
- 函数名里遗留的 `sp.` 前缀（历史导出 API）可逐步别名/弃用，但**新参数名**按上述规则执行。

## 兼容性检查清单（每次空间相关改动）

- 输入：`sf`/`terra` 类型与旧类型是否可识别、转换是否显式且可追踪。
- 坐标：CRS 是否一致，是否存在经纬度与投影坐标混用风险。
- 输出：对象类型、单位（面积/长度）、字段名是否与现有调用一致。
- 影响面：至少检查直接调用方与一个典型下游流程（demo/test/主流程函数）。
