% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface_main.R
\name{auto_build_model}
\alias{auto_build_model}
\title{Automatically Build SHUD Model}
\usage{
auto_build_model(
  project_name,
  domain,
  rivers = NULL,
  dem,
  soil = NULL,
  geology = NULL,
  landcover = NULL,
  forcing_sites = NULL,
  forcing_files = NULL,
  output_dir = getwd(),
  mesh_options = list(),
  river_options = list(),
  aquifer_depth = 10,
  years = 2000:2010,
  clean = TRUE,
  parameters = NULL,
  calibration = NULL,
  melt_factor = NULL,
  remove_outliers = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{project_name}{Character, project name for output files}

\item{domain}{sf object with POLYGON geometry defining watershed boundary}

\item{rivers}{sf object with LINESTRING geometry for river network (optional)}

\item{dem}{SpatRaster object with elevation data}

\item{soil}{SpatRaster object with soil data (optional)}

\item{geology}{SpatRaster object with geology data (optional)}

\item{landcover}{SpatRaster object with land cover data (optional)}

\item{forcing_sites}{sf object with POINT geometry for forcing locations (optional)}

\item{forcing_files}{Character vector of forcing data filenames}

\item{output_dir}{Character, output directory path (default: current directory)}

\item{mesh_options}{List of mesh generation options:
\itemize{
\item max_area: Maximum triangle area (default: 2e5 m²)
\item min_angle: Minimum triangle angle (default: 33 degrees)
\item simplify_boundary: Tolerance for boundary simplification (default: 200 m)
\item simplify_rivers: Tolerance for river simplification (default: 0 m)
}}

\item{river_options}{List of river processing options:
\itemize{
\item min_length: Minimum river segment length (default: 0 m)
}}

\item{aquifer_depth}{Numeric, depth of aquifer bottom below surface (default: 10 m)}

\item{years}{Integer vector, years for LAI/melt factor time series}

\item{clean}{Logical, whether to clean existing files in output directory}

\item{parameters}{List or data.frame, model parameters (default: shud.para())}

\item{calibration}{List or data.frame, calibration parameters (default: shud.calib())}

\item{melt_factor}{xts object, melt factor time series (optional)}

\item{remove_outliers}{Logical, remove outliers in soil/geology data}

\item{verbose}{Logical, display progress messages (default: TRUE)}
}
\value{
List with components:
\item{mesh}{SHUD.MESH object}
\item{river}{SHUD.RIVER object}
\item{attributes}{Attribute data frame}
\item{files}{Named vector of output file paths}
}
\description{
Automated workflow to build a complete SHUD hydrological model from
geospatial data. This modernized function uses terra and sf exclusively
for all spatial operations.
}
\details{
Inputs used for meter-based buffering, simplification, cropping, and mesh
generation must have a defined projected CRS in metres/meters. Longitude/
latitude CRS inputs and projected CRSs in non-metre units are rejected;
transform inputs to an appropriate metre-based projected CRS before calling
this workflow.
}
\section{Migration Note}{

This function replaces \code{autoBuildModel()} with modern spatial libraries.
Key differences:
\itemize{
\item Only accepts terra::SpatRaster and sf objects
\item Rejects legacy spatial objects with clear error messages
\item Legacy inputs belong in deprecated wrapper workflows such as
\code{\link{autoBuildModel}}, which may perform limited conversion before
forwarding to this modern API
\item Uses snake_case parameter names (e.g., project_name vs prjname)
\item Returns structured list instead of just mesh object
\item Provides verbose progress reporting
}

To migrate from legacy code:
\preformatted{
# Modern code (sf/terra):
wbd <- sf::st_read("boundary.shp")
dem <- terra::rast("dem.tif")

# Deprecated autoBuildModel() remains the migration compatibility entry point
# for limited legacy conversion/forwarding.
}
}

\examples{
\dontrun{
library(sf)
library(terra)

# Load data using modern libraries
domain <- st_read("watershed.shp")
rivers <- st_read("rivers.shp")
dem <- rast("dem.tif")
soil <- rast("soil.tif")
lc <- rast("landcover.tif")

# Build model
model <- auto_build_model(
  project_name = "my_watershed",
  domain = domain,
  rivers = rivers,
  dem = dem,
  soil = soil,
  landcover = lc,
  output_dir = "model_output",
  years = 2000:2010
)

# Access results
mesh <- model$mesh
river_network <- model$river
}
}
