---
description: rSHUD 修改流程——先分析根因与方案；R/ 源码须用户确认后再改
alwaysApply: true
---

# rSHUD 修改门禁（Change gate）

## 默认流程（每次改代码前）

1. **说明根因与逻辑**：现象是什么、为何发生（数据路径、API 不一致、依赖、重复定义、CRS/空间对象类型等），用简短因果链写清。
2. **给出修改方案**：拟改哪些文件/函数、预期效果、风险或更保守的替代做法。
3. **再动手改代码**（与方案一致，避免顺带大重构）。

## `R/` 目录（包源码）

- **禁止**在未获用户明确同意的情况下修改 `R/` 下的 `.R` 文件。
- 用户用 **「同意」** / **「同意该方案」** 等明确认可当前方案后，方可修改 `R/`。
- 若用户消息含 **「跳过确认」**，可在完整说明根因与方案后**直接**修改 `R/`（仍应先写清分析与方案，再改代码）。

## 其他路径

- `demo/`、`DESCRIPTION`、`tests/`、`man/`、`vignettes/` 等：仍建议遵循「先分析 → 再方案 → 再改」；若用户仅约束 `R/`，其他目录可在说明方案后按对话意图执行，除非用户另行要求确认。

## 原则

- 最小工程化原则
- 最大兼容性和继承性原则
- 优先**最小可行修改**；避免为修 demo 而在包内做未对齐的大范围重构。
- 回应用户时：**中文**；用户要求复述需求时先简要复述再答。
