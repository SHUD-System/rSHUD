---
description: 自动生成文档保护——禁止修改 man/ 与任何 .Rd 文件
alwaysApply: true
---

# 自动生成文档保护

- `man/` 目录下的任何文件都视为自动生成产物，**禁止直接修改**。
- 任何路径下的 `.Rd` 文件都视为自动生成文档，**禁止修改、创建、删除或重命名**。
- 需要调整帮助文档时，应修改文档源（如 `R/` 中 roxygen 注释或文档生成流程），不要手改 `.Rd`。
- 若用户明确要求修改 `man/` 或 `.Rd`，先提醒这些文件是自动生成的，并建议改文档源或生成流程；默认不要执行对这些文件的写操作。
- 可以读取 `man/` 和 `.Rd` 文件做分析或定位问题，但禁止把它们作为直接编辑目标。
